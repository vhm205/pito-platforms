export enum PartnerStatus {
  IN_PROGRESS = 'inprogress',
  APPROVED = 'approved',
  REJECTED = 'rejected',
}

export enum PartnerType {
  RESTAURANT = 'RESTAURANT',
  COMMERCIAL_KITCHEN = 'COMMERCIAL_KITCHEN',
  KITCHEN_AND_RESTAURANT = 'KITCHEN_AND_RESTAURANT',
}

export enum ServiceType {
  PX = 'PX',
  PC = 'PC',
  PCC = 'PCC',
}
